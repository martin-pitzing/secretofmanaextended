# Scenes Folder

This folder is now the live Godot scene root under `res://scenes/`.

Current contents:

- `main.tscn`: placeholder project entry scene used to verify the shell opens cleanly

Future contents:

- chapter and region scenes
- encounter spaces
- UI-host scenes
- scene-local helper resources
